require("dotenv").config(); // remove this if you are using replit
module.exports = {
    webhook: {
        join: "" || process.env.join, //webhook link where join logs are sent
        leave: "" || process.env.leave, //webhook link where leave logs are sent
    }
}


/* 
  𝖢 O D E D  B Y: 𝖮𝗀𝗀𝗒𝖭𝗈𝗍𝖮𝗉#𝟤𝟧𝟧𝟫
  S U P P O R T | S E R V E R : https://dsc.gg/hypnos-codes
  */